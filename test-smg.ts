const fs = require('fs');

async function test() {
  // We can't easily fetch Google Ads without the proper tokens here in a node script since it relies on next.js env.
  // I will just use the next.js API by adding a temporary debug param to the route!
}
